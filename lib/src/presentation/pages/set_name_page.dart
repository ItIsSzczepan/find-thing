import 'package:flutter/material.dart';

class SetNamePage extends StatelessWidget {
  final Image image;
  const SetNamePage({Key? key, required this.image}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
